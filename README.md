# Test code for a photoQueue System

This photoQueue system is going to be implemented on a wordpress website, so this is simply where i test the code rather than working on it directly on wordpress.

## Test folder
For testing locally

### The Database
The database is just an XAMPP MySQL server.

## Wordpress folder
The file that is live.
